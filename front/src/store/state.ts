export interface State {
	windowWidth: number,
	windowHeight: number,
	scrollY: number,
	isauthed: boolean,
}

const state: State = {
	windowWidth: 0,
	windowHeight: 0,
	scrollY: 0,
	isauthed: false,
}

export default state
